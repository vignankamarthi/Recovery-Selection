"""src/harvest/control package. The control seam: the `RobotBackend` (control + proprioception) and
`SceneOracle` (scene ground truth) interfaces, their `GraspBackend` composition, and the scripted
manipulation policies that drive them. See PLAN.md for the fence."""
